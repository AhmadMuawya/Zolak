<Window x:Class="WaysToSnooze.Zolak.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Zolak"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        Width="64" Height="64"
        Left="0" Top="0">
    <Canvas>
        <!--
            The pet sprite. Mouse events are bound to this Image only,
            so hovering over transparent window area does nothing.
            RenderTransformOrigin is center so ScaleX -1 mirrors correctly.
        -->
        <Image x:Name="PetImage"
               Width="64" Height="64"
               RenderTransformOrigin="0.5,0.5"
               MouseEnter="PetImage_MouseEnter"
               MouseLeave="PetImage_MouseLeave"
               MouseLeftButtonDown="PetImage_MouseLeftButtonDown">
            <Image.RenderTransform>
                <ScaleTransform x:Name="SpriteScale" ScaleX="1" ScaleY="1"/>
            </Image.RenderTransform>
        </Image>
    </Canvas>
</Window>
