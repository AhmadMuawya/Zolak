<Page x:Class="WaysToSnooze.Zolak.Pages.StatesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="Transparent"
      FontFamily="Segoe UI Variable, Segoe UI">

    <!-- Single scroll area for entire page -->
    <ScrollViewer VerticalScrollBarVisibility="Auto"
                  HorizontalScrollBarVisibility="Disabled">
        <StackPanel Margin="24,20">

            <!-- ═══════════════════════════════════════════════════════════
                 HEADER
                 ═══════════════════════════════════════════════════════════ -->
            <StackPanel Margin="0,0,0,14">
                <TextBlock Text="Pet States"
                           FontSize="20" FontWeight="SemiBold"
                           Foreground="{DynamicResource TextPrimary}"/>
                <TextBlock Text="Configure state probabilities, durations, and hover reactions."
                           FontSize="11" Margin="0,3,0,0"
                           Foreground="{DynamicResource TextSecondary}"/>
            </StackPanel>

            <!-- ═══════════════════════════════════════════════════════════
                 CONFIGURATION TABLE
                 ═══════════════════════════════════════════════════════════ -->
            <Border CornerRadius="6" Margin="0,0,0,16"
                    Background="{DynamicResource CardBackground}"
                    BorderBrush="{DynamicResource BorderColor}" BorderThickness="1">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="6" ShadowDepth="1" Opacity="0.15" Color="Black"/>
                </Border.Effect>

                <StackPanel>
                    <!-- Table Header -->
                    <Border Background="{DynamicResource TableHeaderBackground}"
                            CornerRadius="6,6,0,0" Padding="12,0" Height="32">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="65"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="130"/>
                                <ColumnDefinition Width="32"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="State" FontSize="10" FontWeight="SemiBold"
                                       Foreground="{DynamicResource TextSecondary}" VerticalAlignment="Center"/>
                            <TextBlock Grid.Column="1" Text="Chance" FontSize="10" FontWeight="SemiBold"
                                       Foreground="{DynamicResource TextSecondary}" VerticalAlignment="Center"/>
                            <TextBlock Grid.Column="2" Text="Min (s)" FontSize="10" FontWeight="SemiBold"
                                       Foreground="{DynamicResource TextSecondary}" VerticalAlignment="Center"/>
                            <TextBlock Grid.Column="3" Text="Max (s)" FontSize="10" FontWeight="SemiBold"
                                       Foreground="{DynamicResource TextSecondary}" VerticalAlignment="Center"/>
                            <TextBlock Grid.Column="4" Text="Hover Effect" FontSize="10" FontWeight="SemiBold"
                                       Foreground="{DynamicResource TextSecondary}" VerticalAlignment="Center"/>
                            <TextBlock Grid.Column="5" Text="" FontSize="10"
                                       Foreground="{DynamicResource TextSecondary}" VerticalAlignment="Center"/>
                        </Grid>
                    </Border>

                    <!-- Table Rows (no inner scroll — page scrolls) -->
                    <StackPanel x:Name="TableRowsPanel"/>
                </StackPanel>
            </Border>

            <!-- ═══════════════════════════════════════════════════════════
                 STATE CARDS
                 ═══════════════════════════════════════════════════════════ -->
            <TextBlock Text="State Previews" FontSize="13" FontWeight="SemiBold"
                       Foreground="{DynamicResource TextSecondary}" Margin="0,0,0,8"/>
            <WrapPanel x:Name="CardsPanel" Orientation="Horizontal"/>

            <Border Height="20"/>
        </StackPanel>
    </ScrollViewer>
</Page>
